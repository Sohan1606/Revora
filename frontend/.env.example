# Frontend environment (copy to .env.local for dev, or set in Vercel dashboard)
# Empty = same-origin /api (works with the vite dev+preview proxy).
# Production (Vercel): set to your Render backend URL, e.g.
# VITE_API_URL=https://revora-api.onrender.com
VITE_API_URL=
