"""Intelligence layer — pure prediction/estimation.

Contract: features in → scores out. No DB access, no HTTP, no side effects.
Implemented in Phase 8 (scikit-learn baseline → XGBoost, calibrated, versioned).
"""
